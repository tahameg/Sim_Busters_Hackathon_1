# Answer for Question #2 -  Accessing Inherited Functions
## Building
Create a folder named  `build` inside q2 folder
```shell
mkdir build && cd build
```
We are using g++ for building
```shell
g++ ../main.cpp ../A.cpp ../B.cpp ../C.cpp ../D.cpp -o app 
```
## Run the app
```shell
./app
```